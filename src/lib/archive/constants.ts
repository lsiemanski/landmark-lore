/** The default folder every user receives on signup (via the `create_default_folder` DB trigger). */
export const DEFAULT_FOLDER_NAME = "Uncategorized";

/** `photos.status` value for a successfully identified photo. */
export const PHOTO_STATUS_IDENTIFIED = "identified";
